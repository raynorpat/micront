diff --git a/src/files.cpp b/src/files.cpp
index 6ec5a9b..998fb4a 100644
--- a/src/files.cpp
+++ b/src/files.cpp
@@ -372,9 +372,9 @@ BOOL setStdHandle(DWORD nStdHandle, HANDLE hHandle) {
 	return 1; // success
 }
 
-void applyStdHandlesToChildFds() {
-	// Map wibo's SetStdHandle-tracked HANDLEs onto the process's fd 0/1/2
-	// before the child execve's. Only dup2 when the HANDLE is backed by a
+std::vector<std::pair<int, int>> collectStdHandleChildFds() {
+	// Resolve wibo's SetStdHandle-tracked HANDLEs to (sourceFd, targetFd)
+	// dup pairs. Only include a mapping when the HANDLE is backed by a
 	// FileObject whose underlying fd differs from the target std fd.
 	struct Mapping { HANDLE h; int target; };
 	const Mapping map[] = {
@@ -382,6 +382,7 @@ void applyStdHandlesToChildFds() {
 		{stdoutHandle, STDOUT_FILENO},
 		{stderrHandle, STDERR_FILENO},
 	};
+	std::vector<std::pair<int, int>> result;
 	for (const auto &m : map) {
 		if (m.h == NO_HANDLE || m.h == INVALID_HANDLE_VALUE) {
 			continue;
@@ -393,7 +394,16 @@ void applyStdHandlesToChildFds() {
 		if (file->fd == m.target) {
 			continue;
 		}
-		if (dup2(file->fd, m.target) < 0) {
+		result.emplace_back(file->fd, m.target);
+	}
+	return result;
+}
+
+void applyStdHandlesToChildFds() {
+	// Map wibo's SetStdHandle-tracked HANDLEs onto the process's fd 0/1/2
+	// before the child execve's.
+	for (const auto &m : collectStdHandleChildFds()) {
+		if (dup2(m.first, m.second) < 0) {
 			// Can't log via DEBUG_LOG here — we are in the forked child
 			// before execve; use perror for a visible trace.
 			perror("wibo: dup2 std handle");
diff --git a/src/files.h b/src/files.h
index 1ad0aff..accd951 100644
--- a/src/files.h
+++ b/src/files.h
@@ -7,6 +7,8 @@
 #include <filesystem>
 #include <optional>
 #include <string>
+#include <utility>
+#include <vector>
 
 using kernel32::FileObject;
 
@@ -33,6 +35,12 @@ BOOL setStdHandle(DWORD nStdHandle, HANDLE hHandle);
 // is visible to the re-exec'd child. No-op if a std HANDLE is not backed
 // by a FileObject (e.g., pipe/console fallback).
 void applyStdHandlesToChildFds();
+// The (sourceFd, targetFd) dup mappings applyStdHandlesToChildFds() would
+// perform. Exposed for platforms that spawn via posix_spawn (no fork()+exec
+// window to run code in the child): feed each pair to
+// posix_spawn_file_actions_adddup2 so the same redirection — and thus
+// inheritance by grandchildren — happens there too.
+std::vector<std::pair<int, int>> collectStdHandleChildFds();
 std::optional<std::filesystem::path> findCaseInsensitiveFile(const std::filesystem::path &directory,
 															 const std::string &filename);
 std::filesystem::path canonicalPath(const std::filesystem::path &path);
diff --git a/src/processes_darwin.cpp b/src/processes_darwin.cpp
index 2969833..a0d319d 100644
--- a/src/processes_darwin.cpp
+++ b/src/processes_darwin.cpp
@@ -1,6 +1,7 @@
 #include "processes.h"
 
 #include "common.h"
+#include "files.h"
 #include "handles.h"
 #include "kernel32/internal.h"
 
@@ -99,6 +100,20 @@ int spawnProcess(char *const argv[], char *const envp[], SpawnProcessInfo &info)
 	if (rc != 0) {
 		return rc;
 	}
+	// Propagate the parent's SetStdHandle redirections onto the child's
+	// fd 0/1/2, mirroring the Linux fork()+dup2()+execve() path. macOS has
+	// no fork-and-run-code primitive, so do it with spawn file actions.
+	// Without this, shell-style redirection in a cmd-stub-like parent (and,
+	// crucially, captured output of tools that re-exec helpers — e.g.
+	// cl386 -> cl.exe -> c1.exe, or midl -> cl.exe) never reaches the child
+	// or its grandchildren, and leaks to wibo's own stdout instead.
+	posix_spawn_file_actions_t actions;
+	int actionsRc = posix_spawn_file_actions_init(&actions);
+	if (actionsRc == 0) {
+		for (const auto &m : files::collectStdHandleChildFds()) {
+			posix_spawn_file_actions_adddup2(&actions, m.first, m.second);
+		}
+	}
 	sigset_t mask;
 	sigemptyset(&mask);
 	rc = posix_spawnattr_setsigmask(&attr, &mask);
@@ -107,7 +122,10 @@ int spawnProcess(char *const argv[], char *const envp[], SpawnProcessInfo &info)
 	}
 	pid_t pid = -1;
 	if (rc == 0) {
-		rc = posix_spawn(&pid, path.c_str(), nullptr, &attr, argv, envp);
+		rc = posix_spawn(&pid, path.c_str(), actionsRc == 0 ? &actions : nullptr, &attr, argv, envp);
+	}
+	if (actionsRc == 0) {
+		posix_spawn_file_actions_destroy(&actions);
 	}
 	posix_spawnattr_destroy(&attr);
 	if (rc != 0) {
