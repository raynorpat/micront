-- _default — fallback handler for NT TypeNames we don't know yet.
-- Treat as an opaque leaf: no open, no children, no fields, no methods.
-- The Node still renders via its type_name and can be iterated past.

return {}
